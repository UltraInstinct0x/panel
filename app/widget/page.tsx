import Widget from '../_components/Widget';

export default function WidgetPage() {
  return (
    <div style={{ padding: 16, minHeight: '100vh', background: 'var(--bg)' }}>
      <Widget />
    </div>
  );
}
